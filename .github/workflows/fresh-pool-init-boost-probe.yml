name: PumpSwap Fresh Pool Init Boost Probe

on:
  push:
    branches: [audit/boost-runtime-probe-20260815]
    paths:
      - audit/fresh_pool_init_boost_probe.mjs
      - .github/workflows/fresh-pool-init-boost-probe.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - run: npm install --no-save @solana/web3.js@1.98.4 @solana/spl-token@0.4.14
      - name: Simulate true init_boost path on ordinary pool
        run: node audit/fresh_pool_init_boost_probe.mjs
